```bash
apt-get update && apt-get install gh && gh auth login
git clone https://github.com/ostamand/experiments
cd experiments
```

```bash
sudo apt-get update
sudo apt-get install libgl1
git clone https://github.com/Nerogar/OneTrainer.git
cd OneTrainer
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```